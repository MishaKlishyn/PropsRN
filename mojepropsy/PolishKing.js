import { Text,View } from "react-native";

const PolishKing = (props)=>{
    return(
        <View>
            <Text> Polish King is coming {props.name} </Text>
        </View>
    )
}

export default PolishKing;